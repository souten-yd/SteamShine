# Implementation status

- Baseline: `master` after PR #8 (`0273a1149348669e458cbb0aa1b83ad3020db720`)
- Last successful PR #8 full validation: run `30248822908`, head `d3aa9b8f9972efb1c33cf837781311c650075da3`
- Future-work source of truth: `PROJECT_ROADMAP.md` and `STREAM_NEGOTIATION_HDR_QUALITY_DESIGN.md`

| Item | Status | Evidence and remaining gate |
| --- | --- | --- |
| SteamShine service and Artifact lifecycle | Hardware tested and CI tested | User-local immutable Artifact installation, systemd user service, BUILD_INFO provenance, rollback versions, and SteamOS compatibility checks exist. Continue testing only the matching Artifact and service process. |
| SteamShine-primary management direction | Adopted; default-route switch gated | SteamShine is the project/server identity and `/steamshine/` is the primary management implementation. The inherited Sunshine UI remains the shared-backend backup route until hardware acceptance and rollback requirements are complete. |
| SteamShine Web UI redesign | Merged; hardware acceptance pending | PR #8 added Monitor, Applications, Settings, GPU/CPU profiles, PTY Terminal, responsive navigation, and independent branding. Full PR CI passed. Real SteamOS sensor values, capability-backed sysfs writes, Terminal use, LAN access, and streaming-time overhead remain hardware gates. |
| Shared Web backend and Sunshine backup | CI/browser tested; hardware coexistence pending | SteamShine and upstream Sunshine use shared credentials, pairing, clients, applications, and configuration. Security and browser smoke coverage exists. Verify both routes on localhost/LAN and while streaming, then test configuration-only rollback. |
| Physical desktop source selection | Hardware and Moonlight tested; latest release revalidation pending | Automatic physical selection, KWin ScreenCast, bounded Portal timeout, black fallback initialization, and live Moonlight video/audio/input were observed. Repeat with the next delivery Artifact. |
| KWin PipeWire format negotiation | Hardware and Moonlight tested | KWin rejected the former all-zero max-framerate offer. The current path advertises variable-rate capture with a positive requested-FPS maximum and fails if no real format is selected. |
| Client-sized physical Desktop and Big Picture | Hardware and Moonlight tested | The packaged KScreen hook selected an exact 1920x1080@60 mode for a Moonlight request and restored 3440x1440@100 with scale 1.5 afterward. Unsupported physical dimensions currently fail closed; the next design adds owned-virtual fallback policy. |
| Moonlight request propagation | Partially implemented | Width, height, integer FPS, HDR intent, client identity, and app/audio/input options reach `launch_session_t`; client geometry/FPS/HDR are exported to application preparation. Requested/selected/active/observed state is not yet unified. |
| Owned virtual resolution/FPS | Implemented within current limits; broader negotiation pending | Width 640–7680, height 480–4320, and FPS 30–240 are normalized and passed to headless Gamescope. Integer FPS and retained-session compatibility need refinement; arbitrary safe requests require explicit active-state reporting and source-aware fallback tests. |
| HDR | Partially wired; end-to-end acceptance not complete | Client HDR intent exists, display-device automatic HDR concepts exist, and owned Gamescope adds `--hdr-enabled` when advertised. Capture metadata, 10-bit conversion, encoder profile, GameStream signaling, SDR fallback, and client rendering are not yet validated as one transaction. |
| H.264/HEVC/AV1 | Existing encoder paths and probes; policy integration pending | Video configuration already exposes HEVC/AV1 modes, Vulkan rate-control settings, quality controls, max bitrate, FEC, and packet size. SteamShine still needs one probe-driven codec/profile/bit-depth selection policy and measured AV1 acceptance. |
| Gamescope/PipeWire same-GPU capture | Hardware tested; current release revalidation pending | Verified Gamescope `Video/Source`, PID/UID/start-time identity, DMA-BUF capture, RX 9070 XT Vulkan Video, and positive frame/packet/byte/IDR counters have been observed. Preserve `/dev/dri/renderD128` and no CPU full-frame fallback. |
| Existing Game Mode source ownership | Implemented; full hardware matrix pending | Existing Gamescope discovery, ownership flags, Steam singleton protection, and detach-without-kill behavior are merged. Verify live Game Mode video, input, reconnect, and service-stop behavior with the next Artifact. |
| Gamescope input isolation | Unit/local tested; hardware revalidation pending | Verified `gamescope-*-ei` ownership and libei routing exist for mouse, keyboard, scroll, and pointer emulation. Game Mode fails closed if EIS cannot be verified. |
| Bounded input/video/network latency | Local stress tested; Moonlight hardware revalidation pending | Edge-preserving bounded input, motion coalescing, bounded capture/encoder/network flow, fixed-memory latency rings, and aggregate JSON reports are merged. A 1 kHz 60-second slow-consumer test held queue limit 64 and recovered below 100 ms. Verify the original continuous-operation symptom on the official Artifact. |
| IDR accounting | Local/unit tested; hardware correlation pending | Client, recovery, periodic, and reconnect reasons are counted; duplicate client requests are rate-limited. Correlate IDR bursts with loss, queues, and visible stutter on hardware. |
| Input Latency Visualizer | Built and packaged | Fullscreen event-driven visual feedback, timestamps, sequence/frame counters, joystick activity, and headless self-test are available. Use it for 60-second service-mode acceptance. |
| Adaptive bitrate | Detailed design only | Existing max-bitrate/FEC/packet and latency metrics will be reused. The 500 ms/2 s controller, runtime rate adapter, hysteresis, congestion state, and learned next-session start bitrate are not implemented. |
| Per-client profiles and safe area | Detailed design only | Generic, 4K, Y700, iPhone 16 Plus, Deck LCD/OLED, network-class persistence, safe area, and requested/selected/observed comparison remain future work. Profiles must not override actual client capabilities. |
| Zero-copy local presenter | Partial infrastructure only | Independent PipeWire consumer, latest-frame queue, DMA-BUF lifetime handling, host Wayland validation, and xdg-shell surface exist. Vulkan external-memory import, swapchain present, hotplug, and dual-consumer hardware acceptance remain incomplete. |
| Hardware latency/quality/SSD matrix | Blocked on next implementation Artifact | Collection and visualizer infrastructure exist. Run arbitrary geometry/FPS, HDR, H.264/HEVC/AV1, Ethernet/Wi-Fi, continuous input, reconnect, power, and write comparisons as defined in the canonical roadmap. |
